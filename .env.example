# PostgreSQL connection used by Drizzle and server database access
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Encryption key used by nuxt-auth-utils for session cookies.
# Must contain at least 32 characters.
NUXT_SESSION_PASSWORD=replace-with-a-random-string-at-least-32-characters-long
